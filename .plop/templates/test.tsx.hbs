import { render, screen } from '~/utils/tests'

import { {{pascalCase name}} } from '.'

describe('<{{pascalCase name}} />', () => {
  it('renders', () => {
    render(<{{pascalCase name}} prop="{{lowerCase name}}" />)

    expect(screen.getByRole('heading', { name: /{{lowerCase name}}/i })).toBeInTheDocument()
  })
})
